// StoreView - placeholder for store view (to be implemented in later step)

import React from 'react';
import { View, Text } from 'react-native';

export const StoreView = (): React.JSX.Element => (
  <View>
    <Text>Store View</Text>
  </View>
);
